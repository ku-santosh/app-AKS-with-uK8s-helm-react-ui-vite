apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "react-ui.fullname" . }}-config
  labels:
    environment: {{ .Values.env }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}
