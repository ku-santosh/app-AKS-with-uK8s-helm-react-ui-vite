# ============================================================
# RECSDI-UI Service Definition (Commented)
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: recsdi-ui-svc
  namespace: at41457-dev-recsdiui-dev
  labels:
    app: recsdi-ui
    environment: dev
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: recsdi-ui
